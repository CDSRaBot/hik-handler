@hik-handler.project/
├── app/                        # Основной пакет приложения
│   ├── __init__.py             # Инициализация пакета для абсолютных импортов
│   ├── communication/          # Сетевой слой (Транспорт)
│   │   ├── __init__.py
│   │   └── network.py          # HikvisionClient (ISAPI, Digest Auth)
│   ├── engine/                 # Бизнес-логика и оркестрация
│   │   ├── __init__.py
│   │   ├── logger.py           # Централизованное логирование
│   │   ├── orchestrator.py     # Главный координатор (Engine)
│   │   └── resolver.py         # ArgumentResolver (${var})
│   ├── parser/                 # Обработка ответов
│   │   ├── __init__.py
│   │   └── parser.py           # ResponseParser (XML)
│   ├── user_interface/         # Интерфейс взаимодействия (CLI/UI)
│   │   ├── __init__.py
│   │   └── cli.py              # Обработка аргументов и вывод
│   └── validator/              # Валидация данных и контрактов
│       ├── __init__.py
│       └── validator.py        # ModuleValidator (XSD)
├── logs/                       # Журналы работы системы
│   └── hik-handler.log
├── modules/                    # XML-описания команд устройств
│   └── *.xml
├── config.toml                 # Конфигурация среды (TOML формат)
├── hik-handler.py              # Точка входа (Lightweight Entry Point)
├── modules.xsd                 # Схема валидации модулей
└── pyproject.toml              # Манифест проекта и зависимости