# Archivist Log: Release v.1.1.0 (17.05.2026)
- **Core Stability:** Fixed critical `AttributeError` and `TypeError` in `Orchestrator` and `CLITerminal`.
- **Security:** Hardened XML parsing (`defusedxml`) and enforced non-empty password policy.
- **UX/Infra:** Added interactive password prompt and automated `sandbox` directory management.
- **Code Quality:** Enforced PEP 8, structured logging, and English-only documentation throughout the system.
